# 02 — Repository Structure

This document defines the desired repository structure for both the loomstack framework repo and generated loomstack apps.

## loomstack framework repository

```txt
loomstack/
  package.json
  pnpm-workspace.yaml
  tsconfig.base.json
  vitest.config.ts
  AGENTS.md
  CLAUDE.md

  packages/
    cli/
      package.json
      src/
        index.ts
        commands/
        output/
      tests/

    core/
      package.json
      src/
        manifest/
        graph/
        errors/
        project/
      tests/

    runtime/
      package.json
      src/
        schema/
        action/
        query/
        policy/
        view/
        context/
      tests/

    generator/
      package.json
      src/
        app/
        feature/
        routes/
        context/
        files/
      tests/
      snapshots/

    verifier/
      package.json
      src/
        rules/
        imports/
        generated/
        manifests/
      tests/

    react-adapter/
      package.json
      src/
        client/
        routing/
        hooks/
        view/
      tests/

    koa-adapter/
      package.json
      src/
        server/
        routes/
        context/
        errors/
      tests/

    postgres-adapter/
      package.json
      src/
        db/
        migrations/
      tests/

    create-loomstack-app/
      package.json
      src/
        index.ts

  templates/
    default-app/
      package.json.tpl
      apps/
      features/
      loomstack.config.ts

  examples/
    manager-crm/

  docs/
```

## Generated loomstack app structure

Generated apps should be boring and deterministic.

```txt
my-app/
  package.json
  pnpm-workspace.yaml
  tsconfig.base.json
  loomstack.config.ts
  AGENTS.md
  CLAUDE.md

  .loomstack/
    context.json
    graph.json
    commands.json
    errors.json
    generated-files.json

  apps/
    web/
      package.json
      index.html
      src/
        main.tsx
        app.tsx
        routes.generated.tsx
        api-client.generated.ts

    api/
      package.json
      src/
        server.ts
        routes.generated.ts
        context.ts

  features/
    people/
      feature.yaml
      AGENTS.md
      model.schema.ts
      permissions.policy.ts
      actions/
        create-person.action.ts
        update-person.action.ts
        delete-person.action.ts
      queries/
        list-people.query.ts
        get-person.query.ts
      ui/
        people-list.view.tsx
        person-detail.view.tsx
        person-form.view.tsx
      tests/
        create-person.test.ts
        permissions.test.ts

  shared/
    generated/
      feature-registry.generated.ts
      schema-registry.generated.ts
      action-registry.generated.ts
      query-registry.generated.ts
```

## App-level file responsibilities

### `loomstack.config.ts`

Contains project configuration:

```ts
import { defineloomstackConfig } from "@loomstack/runtime"

export default defineloomstackConfig({
  appName: "manager-crm",
  packageManager: "pnpm",
  frontend: "react",
  backend: "koa",
  database: "postgres",
  featuresDir: "features",
  generatedDir: ".loomstack"
})
```

### `.loomstack/context.json`

Machine-readable summary for agents.

Generated by:

```bash
loomstack context generate
```

### `.loomstack/graph.json`

Machine-readable feature and dependency graph.

Generated by:

```bash
loomstack graph generate
```

### `.loomstack/commands.json`

Machine-readable command registry.

Contains commands agents should use:

```json
{
  "dev": "pnpm dev",
  "test": "pnpm test",
  "typecheck": "pnpm typecheck",
  "verify": "pnpm loomstack verify"
}
```

### `.loomstack/generated-files.json`

Manifest of generated files and hashes.

Used by the verifier to detect stale or hand-edited generated files.

## Generated file rules

All generated files must:

1. Start with a generated marker.
2. Be deterministic.
3. Be safe to overwrite.
4. Be listed in `.loomstack/generated-files.json`.
5. Be excluded from manual business logic.

Marker:

```ts
// GENERATED BY loomstack. DO NOT EDIT.
```

## Feature file naming rules

Required patterns:

```txt
feature.yaml
AGENTS.md
model.schema.ts
permissions.policy.ts
actions/*.action.ts
queries/*.query.ts
ui/*.view.tsx
ui/*.component.tsx
ui/*.form.tsx
tests/*.test.ts
```

## Dependency direction

Allowed conceptual dependency flow:

```txt
schema -> policy -> action/query -> view
```

Adapter-generated code may import action/query/view registries.

Feature UI may not import backend code or database code.

## Non-goal

Do not support arbitrary app structures in v0.1. loomstack should enforce the structure it generates.
