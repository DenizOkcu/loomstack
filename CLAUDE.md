# loomstack agent memory

loomstack reduces the search space for coding agents.

```text
React = presentation
Koa = transport
features/* = product behavior
```

Edit authored feature contracts, not generated wiring. Keep output deterministic, commands machine-readable, and errors repairable. Run `pnpm test`, `pnpm typecheck`, and `pnpm build` after framework changes. Do not introduce v0.1 non-goals.
